{
  "scripts": {
    "dev": "encore dev",
    "watch": "encore dev --watch",
    "build": "encore production",
    "dev-server": "encore dev-server",
    "sync": "browser-sync start --proxy '127.0.0.1:8000' --files 'templates/**/*.twig' 'assets/**/*.js' 'assets/**/*.scss' 'public/build/**/*.css' 'public/build/**/*.js'"
  },
  "devDependencies": {
    "@symfony/webpack-encore": "^5.1.0",
    "bootstrap": "^5.3.4",
    "browser-sync": "^3.0.4",
    "sass": "^1.86.2",
    "sass-loader": "^16.0.5",
    "webpack-dev-server": "^5.2.1",
    "webpack-notifier": "^1.15.0"
  },
  "dependencies": {
    "core-js": "^3.42.0"
  }
}
